<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.managenyama">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManageNyama">
        <activity android:name=".MuttonStock"></activity>
        <activity android:name=".FishStock" />
        <activity android:name=".ChickenStock" />
        <activity android:name=".PigStock" />
        <activity android:name=".GoatStock" />
        <activity android:name=".BeefStock" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Profile"
            android:label="Profile" />
        <activity
            android:name=".Sales"
            android:label="Sales" />
        <activity
            android:name=".Stock"
            android:label="Stock" />
        <activity
            android:name=".HomeScreen"
            android:label="Home" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>